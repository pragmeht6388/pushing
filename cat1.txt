  <mat-error *ngIf="myForm.get('myTextField').hasError('minlength')">
      Minimum length is 3 characters
    </mat-error>

For improved performance, use the LoggerMessage delegates instead of calling 'LoggerExtensions.LogDebug(ILogger, string?, params object?[])'



 private static readonly Action<ILogger, string, Exception?> _logDebugMessage =
        LoggerMessage.Define<string>(
            LogLevel.Debug,
            new EventId(1, "DebugMessage"),
            "Processing request: {RequestId}");

    private readonly ILogger<SomeService> _logger;

    public SomeService(ILogger<SomeService> logger)
    {
        _logger = logger;
    }

    public void ProcessRequest(string requestId)
    {
        // Use the LoggerMessage delegate instead of LogDebug
        _logDebugMessage(_logger, requestId, null);

        // Perform other processing...
    }
}
