ReturnValue<IDictionary<string, string>> result = GetSomeValue();
if (result.IsSuccess)
{
    var dict = result.Value;
    if (dict.TryGetValue("email", out var email))
    {
        Console.WriteLine(email);
    }
}


dynamic result = GetSomeValue();
var dict = (IDictionary<string, string>)result.Value;
string email = dict["email"];
