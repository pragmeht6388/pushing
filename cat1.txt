      Quote quote = GetQuote(quoteQti, qtiModel);
      _logger.Debug($"quote.QTINumber=>{quote.QTINumber}");
      quote.StatusId = GetReference("QUOTE_STATUS", quoteQti.QTI_STATE, qtiModel.CountryId).FirstOrDefault().ReferenceId;

'GetReference("QUOTE_STATUS", quoteQti.QTI_STATE, qtiModel.CountryId).FirstOrDefault()' is null on at least one execution path.

var reference = GetReference("QUOTE_STATUS", quoteQti.QTI_STATE, qtiModel.CountryId).FirstOrDefault();
if (reference != null)
{
    quote.StatusId = reference.ReferenceId;
}
else
{
    // Handle the case when reference is null
    _logger.Warn($"No matching reference found for QUOTE_STATUS with QTI_STATE: {quoteQti.QTI_STATE} and CountryId: {qtiModel.CountryId}");
    // Optionally, set a default value for StatusId, log an error, or handle it in another way
    quote.StatusId = GetDefaultStatusId();  // Use a default value if necessary
}

