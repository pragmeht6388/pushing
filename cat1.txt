        catch (TimeoutException timeoutEx)
        {
            Log.Logger.Error(timeoutEx, "Timeout occurred while trying to convert prospect {0} to driver.", prospect.Email);
            Console.WriteLine("Timeout error: " + timeoutEx.Message + " Email: " + prospect.Email);
            throw;
        }

Either log this exception and handle it, or rethrow it with some contextual information. (csharpsquid:S2139 )

catch (TimeoutException timeoutEx)
{
    var message = $"Timeout occurred while trying to convert prospect {prospect.Email} to driver.";
    Log.Logger.Error(timeoutEx, message);
    Console.WriteLine("Timeout error: " + timeoutEx.Message + " Email: " + prospect.Email);
    
    throw new TimeoutException(message, timeoutEx);
}
