var prospects = await service.GetAllProspects().ConfigureAwait(false);

Dereference of a possibly null reference. (external_roslyn:CS8602)

var service = host.Services.GetService<IRoleService>();
var prospects = await service.GetAllProspects().ConfigureAwait(false);

Console.WriteLine(prospects.Value.Count());
Console.ReadLine();

var service = host.Services.GetService<IRoleService>();

if (service == null)
{
    Console.WriteLine("IRoleService is not registered in the service provider.");
}
else
{
    var prospects = await service.GetAllProspects().ConfigureAwait(false);
    Console.WriteLine(prospects.Value.Count());
}

Console.ReadLine();
