Use model binding instead of accessing the raw request data

    [HttpGet("GetCashFlowSavings/{quoteId}")]
    public Task<dynamic> GetCashFlowSavings(int quoteId)
    {
        var jwtToken = Request.Headers["Authorization"];
        return QuotationService.GetCashFlowSavings(quoteId, jwtToken, 20);

    }
