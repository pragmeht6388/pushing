   if (Url.IsLocalUrl(model!.ReturnUrl) && allowedUrls.Contains(model!.ReturnUrl))
   {
       return Redirect(model!.ReturnUrl);
   }

var returnUrl = model!.ReturnUrl.ToString(); // OR use model!.ReturnUrl.AbsolutePath if it's a Uri
if (Url.IsLocalUrl(returnUrl) && allowedUrls.Contains(returnUrl))
{
    return Redirect(returnUrl);
}
